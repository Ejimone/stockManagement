import { Text } from "react-native";

export default function HomeScreen() {
  // there should be nothinn the top of thr screen
  // the only content should be a text "Hello World"
  return <Text>Hello World</Text>;
}
